name: Test Suites

on:
  push:
    branches: 
      - development
      - 'dependabot/*'
  pull_request:
    branches: 
      - master
      - development
jobs:
  testfront:
   name: Test Frontend
   runs-on: ubuntu-latest

   env:
     client: ./www

   steps:
     - name: Checkout
       uses: actions/checkout@v2

     - name: Node Setup
       uses: actions/setup-node@v1
       with:
        node-version: 12

     - name: NPM Install
       run: npm ci
       working-directory: ${{env.client}}

     - name: Test Frontend
       run: npm test
       working-directory: ${{env.client}}

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [testfront]

    env:
     client: ./www

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Node Setup
        uses: actions/setup-node@v1
        with:
          node-version: 12

      - name: NPM Install
        run: npm ci
        working-directory: ${{env.client}}

      - name: Build Frontend
        run: npm run build
        working-directory: ${{env.client}}
